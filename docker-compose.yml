services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DEBUG=True
      - ENVIRONMENT=development
    volumes:
      - .:/app
    command: python manage.py runserver 0.0.0.0:8000
